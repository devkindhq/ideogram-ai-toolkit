# Examples

This skill's worked examples live at the repo root rather than duplicated here,
since they were the original demonstration of the JSON-caption / prompting
technique this skill documents, before the per-skill layout existed:

- [`../../../examples/README.md`](../../../examples/README.md) — 6 real generated
  examples covering precise `color_palette` control, `art_style` + in-image text
  via bounding boxes, `graphic_design` medium, `3d_render` medium, and the full
  reference-image style-extraction workflow (`describe_image` → new subject).

If a future real job demonstrates a technique the root set doesn't already cover,
add it here as its own `examples/<name>.md` following the sibling-skill pattern
instead of growing the root set further.
